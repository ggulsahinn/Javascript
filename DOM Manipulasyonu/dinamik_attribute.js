const todoInput = document.getElementById("todo")
let element

element = todoInput
element = todoInput.classList

// todoInput.className = "form-control newClass"

// todoInput.classList.add("newClass")
// todoInput.classList.add("newClass2")
// todoInput.classList.remove("newClass2")

element = todoInput
element = todoInput.placeholder
element = todoInput.getAttribute("placeholder")
todoInput.setAttribute("placeholder","Naber")
todoInput.setAttribute("title","Input")
todoInput.removeAttribute("id") //id özelliğini kaldırır

element = todoInput
element = todoInput.hasAttribute("name") //attribute var mı kontrol eder



console.log(element)